# Matching

How the app scores a job against the consultant profile, how the old engine worked, and how
the new engine is measured against it. Code: `core/src/matching`. Tests:
`core/tests/matching_legacy.rs`, `core/tests/matching_corpus.rs`, `core/tests/common/eval.rs`.

## The old engine (v3, `ca9a2cd^:matcher.py`)

Job-driven: requirements are extracted from the ad (independent of the profile) and each is
checked against the profile's core phrases. Reproduced exactly by `matching::legacy`.

| Step | Old behaviour |
|---|---|
| Normalise | Python `casefold` (ß -> ss); tokens `[a-z0-9äöüß+#][a-z0-9äöüß+#./\-]*`, trailing dots stripped; keep length >= 3 or a short synonym key; 246 stopwords |
| Synonyms | 47 word synonyms applied first (`hana` dropped), then 19 adjacent pairs (2 can never fire: `s4 hana`, `agile methoden`) |
| Profile | Strings under 12 core keys or 14 skill keys (any depth, key casefolded, 23 personal keys skipped) plus 7 known lists (`kernkompetenzen[].kompetenz`, `methoden_tools[].name`, ...). Only whole core phrases can meet a requirement; free text never does |
| Sections | Line headings (<= 64 chars) looked up NEUTRAL (33), NICE (28), MUST (54), TASK (56), then 8 prefixes; bullet lines are never headings; only MUST/NICE content counts, split into sentences after `.!?;` + whitespace before `[A-ZÄÖÜ0-9]` |
| Inline headings | `Anforderungen: ...` on one line: the old code raised `IndexError` (one regex group, two read) and the whole run failed |
| Fallback 1 | No must: requirement sentences with cues (`vorausgesetzt`, `mehrjährig`, `Erfahrung ... in/mit`, ...) anywhere; `von Vorteil` etc. makes them nice. Sections are ignored here, so nice lines become musts |
| Fallback 2 | Still no must: 234 vocabulary terms found in the text, longest first, contained shorter terms dropped |
| Items | Requirement split at ` sowie / und / oder / bzw. / beziehungsweise `, `, `, `; `, ` & ` (`oder` is an extra item, not an alternative); items without core tokens are skipped; `...kenntnisse` adds the language stem |
| Match | (i) phrase contiguous in the item; (ii) one-token item equal to or at most 2 chars longer than a one-token phrase; (iii) phrase of >= 3 tokens with 2/3 present = 0.5, which still counts as met |
| Score | `round(100 * base * 0.7^violations)`; base = 0.75 must + 0.25 nice, must only, 0.6 nice (nice only) or found/all (vocabulary); Python floats and half-to-even; nothing assessable = job absent; texts under 100 chars absent |
| Signals | remote 100 / n % / 0 on site / 60 if "remote" appears; rate after `Tagessatz/Honorar/...` or `NNN € pro Tag` with `.`/`,` removed; ANÜ named and no negated mention anywhere; 24 country names anywhere -> 13 ISO codes |
| Criteria | `harte_kriterien.{min_tagessatz (or einsatzpraeferenzen.tagessatz_ab), laender, ausgeschlossene_vertragsarten, verfuegbar_ab (or einsatzpraeferenzen), remote_ausserhalb_erlaubt}`; missing key = inactive; violations: ANÜ, rate < minimum, "sofort" + `ab <Monat>`, any named country outside the list unless 100 % remote and allowed |

Known weaknesses the corpus exposes: rate ranges read the tail (`900 - 1.100 €` -> 100 €),
negations only directly before the word (`nicht im Rahmen der ANÜ` counts as ANÜ), countries
anywhere (a US headquarters excludes a remote job), a later start is a violation, compounds
and inflections fail (`Carve-out-Projekten`, `Restrukturierungsumfeld`), English wording
never meets German skills, soft skills weigh like hard skills, `oder` alternatives are all
required, and inline headings crash the run.

## Parity of the port

The port copies Python's semantics instead of approximating them: casefold, `isspace`,
`splitlines` and the `re` classes `\w`/`\d` come from `core/src/matching/python_unicode.txt`
(generated by Python 3.14, Unicode 16.0); case-insensitive patterns run on a one-to-one view of
the text so the `regex` crate sees Python's case equivalences and word boundaries; the score
repeats Python's float operations (exact integer arithmetic with half-to-even rounding differs
from the old engine in ordinary cases, for example 85 % x 0.7 = 59.5 -> 60 against Python's 59).

| Check | Result |
|---|---|
| 35 old unit tests, same numbers (`matching_legacy.rs`) | 35 / 35 green |
| `legacy_percent` == `legacy.json` (2 profiles x 40 ads) | 80 / 80 identical |
| Edge set (Unicode, splitting, criteria, crash; 3 profiles x 9) | 27 / 27 identical |
| Real local ads (6 files x 2 sample profiles, not committed) | 12 / 12 identical |

One documented deviation: `_int_rate("1.200,50")` is 1200 in Rust (a trailing `,d`/`,dd` is a
decimal comma); the old engine read 120050. No corpus, edge or local text contains such a rate.

`legacy.json` and `legacy_edge.json` are frozen by `py -3.14 tools/eval/legacy_baseline.py
corpus|edge` (old code via `git show ca9a2cd^`, one job per `run_match` call so a crash only
marks that job); the script refuses to change them.

## Corpus and gates

`core/tests/fixtures/matching`: two invented profiles (`sample_profile.json` interim finance,
`sample_profile_it.json` SAP/IT project lead with the `tagessatz_ab` fallback and a later
availability) and 40 invented ads K01-K40 in the TXT contract format. `corpus.json` fixes per
job and profile, before any engine work and after an independent review: band (width <= 30),
status (excluded > unscorable > scored), decided violations (anue, country, dayRate),
expected check codes (anueOptional, anueHidden, countryUnclear, dayRateCurrency, startVague,
availabilityGap, permanent, formalOpen, lowEvidence, shortText) and musts that must stay open.

Gates (`matching_corpus.rs`; the new-engine gates switch on in phase 2A):
- parity: `legacy_percent` == `legacy.json`, 35 old tests green;
- per job and profile: band distance of the new score <= that of the old score (absent or
  unscorable = 0), and the sum strictly smaller;
- decided exclusions and status exactly as in `corpus.json`; expected checks raised; `mustOpen`
  items never met;
- no fixture ignored by git.

Metrics for the private gold set (`common/eval.rs`): NDCG@k (gain 2^grade - 1), P@k,
Spearman, high-band precision, paired bootstrap with a fixed seed.

## New vs old

(filled in phase 2A)
