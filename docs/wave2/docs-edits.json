{
 "edits": [
  {
   "file": "README.md",
   "where": "line 12, 'What it does'",
   "problem": "Uses em dashes as separators ('rule engine — nothing is sent'). Also at lines 20, 55, 76 and 86. CLAUDE.md bans this style for UI text, and the README should read the same way.",
   "change": "Replace each ' — ' with a colon, comma or full stop. For example: 'Scores every job locally against a consultant profile with an explainable, integer-only rule engine. Nothing is sent to the cloud.' Line 20: 'The build is unsigned, so Windows SmartScreen blocks it on first launch: choose More info -> Run anyway.'"
  },
  {
   "file": "README.md",
   "where": "lines 32-34, 'First start'",
   "problem": "The app can now switch between German and English, but the README does not say so. It gives the German label only.",
   "change": "Append: 'The app starts in German; Einstellungen > Sprache switches it to English at once (the button is then Fetch).'"
  },
  {
   "file": "README.md",
   "where": "lines 40-42, 'The profile'",
   "problem": "The CV prompt no longer names Claude. It says 'für eine KI', the privacy note reads 'Der Lebenslauf geht an die KI, die du nutzt.', and there is no Claude-specific wording in core/src/profile/prompt.rs.",
   "change": "Replace with: '\"Aus Lebenslauf erstellen\" copies a prompt for the AI chat you use; its answer, pasted back, fills the form for review.'"
  },
  {
   "file": "README.md",
   "where": "lines 43-48, 'The profile'",
   "problem": "The README says the profile uses German keys only. The code also reads English keys and writes them back where they are. Since engine 13, country names are also accepted.",
   "change": "Change the first sentence to: 'The consultant profile is a JSON file with German keys (English keys are read too and kept where they are), in the structure shown by ...'. After the `auch` sentence add: 'Allowed countries are ISO codes or country names (\"DE\" or \"Deutschland\"); a value the app cannot read is shown at its field and is no rule.'"
  },
  {
   "file": "README.md",
   "where": "lines 55-58, 'How matching works'",
   "problem": "The README says availability can exclude a job. It cannot: facts.rs raises AvailabilityGap with decided=false, so it is a check only. The list also leaves out real exclusions: permanent employment as an excluded contract type (engine 11), tooJunior, and formalOpen when worded as mandatory. It says nothing about Schwerpunkte, wishes or target roles either.",
   "change": "Replace the sentence 'Hard criteria (ANÜ, country, day rate, availability, and — for permanent roles — minimum salary and region) are evaluated separately and can exclude a job outright.' with: 'Hard criteria from the profile exclude a job only on clear wording: an excluded contract type (temporary agency work, permanent employment), a country outside the allowed ones, a day rate below the minimum, for permanent roles a salary below the minimum or a place outside the region, a target profile below the wanted seniority, a formal requirement stated as mandatory. Unclear wording and the start date become points to check. Schwerpunkte, target roles and wishes move the score but never exclude.'"
  },
  {
   "file": "README.md",
   "where": "lines 61-70, the measurement table",
   "problem": "The table shows engine-3 numbers on a 52-ad corpus. The corpus now has 58 ads and the engine is at version 13 (core/src/matching/mod.rs:68). The seven held-out sets are not mentioned.",
   "change": "Replace the intro and table with: 'Measured on the synthetic corpus (58 invented ads, bands fixed before the engine work; engine 13 against the old engine; in band = score inside the agreed band, distance = sum of the distances to the band, Spearman = rank correlation of score and band):' followed by this table:\n| Profile | Jobs | In band, old | In band, new | Distance, old | Distance, new | Spearman, old | Spearman, new |\n|---|---|---|---|---|---|---|---|\n| fin (interim finance) | 58 | 26/58 | 56/58 | 613 | 11 | 0.66 | 0.87 |\n| it (SAP/IT project lead) | 58 | 45/58 | 56/58 | 241 | 11 | 0.66 | 0.85 |\n| senior (interim CFO) | 58 | 22/58 | 55/58 | 611 | 13 | 0.69 | 0.74 |\n| sap (SAP FI/CO consultant) | 58 | 40/58 | 56/58 | 229 | 6 | 0.73 | 0.85 |\nThen add: 'On seven held-out sets (invented ads with blind labels by independent agents, 192 to 960 pairs each), NDCG@10 is 0.82 to 0.96, against 0.35 to 0.57 for the old engine. The sets were later used to fix general gaps, so they are now regression gates, not an unseen measurement. Real ads are still to be measured (docs/PLAN.md, phase 5).' (Numbers were computed from `matching_corpus -- --ignored report` and `matching_heldout -- --ignored heldout_report` at a2b2353.)"
  },
  {
   "file": "README.md",
   "where": "lines 74-79, 'Privacy and portals'",
   "problem": "The README names only the stage-2 skill as a way data reaches an AI. It leaves out the app's own copy-prompt features: ai_prompt, ai_prompt_top and profile_prompt.",
   "change": "Replace the first sentence with: 'Everything runs locally: jobs, full texts, the database and logs never leave the machine except for the IMAP connection to Gmail and the HTTP requests to the job portals themselves. The app sends nothing to an AI: the prompts it copies (one job, the best matches, a profile from a CV) go only where you paste them, and a job prompt carries the profile without name, contact data and links. The optional stage-2 skill runs in your own Claude, without an API key.'"
  },
  {
   "file": "README.md",
   "where": "lines 83-87, 'Portal notes'",
   "problem": "The README mentions 'an account-risk warning in the UI'. The risk grades were removed in 573fa57; PortalCard.svelte now reads 'no risk grades' and the sign-in row is 'Mit Anmeldung' with the hint 'Zeigt ganze Anzeigen statt eines Anrisses.' The README also does not give the raised limits.",
   "change": "Replace from 'freelance.de sign-in is off by default' to the end of the paragraph with: 'freelance.de shows guests only a teaser; signing in (Einstellungen, Portale, Mit Anmeldung) is off by default, because freelance.de's crawling policy does not clearly permit signed-in scraping. Every portal can be switched off, or kept for its alert mails without fetching pages. All portals are fetched conservatively: at most 30 pages per hour and 80 per day from LinkedIn, 40 and 120 from freelancermap, 20 and 60 from freelance.de, randomized pauses between pages, and an automatic stop on rate limits, blocks, captchas or sign-in walls, never bypassed.'"
  },
  {
   "file": "README.md",
   "where": "line 91, 'Requirements'",
   "problem": "The README says 'Node 24'. package.json declares \"engines\": {\"node\": \">=22.12\"} and CI uses node-version lts/*.",
   "change": "'**Requirements:** the Rust toolchain pinned in `rust-toolchain.toml`, Node 22.12 or newer (CI uses the current LTS), npm.'"
  },
  {
   "file": "README.md",
   "where": "lines 93-103, 'Commands'",
   "problem": "The harness needs its browsers installed first, and the README does not say so. The debug smoke probe and the matching reports that CLAUDE.md and MATCHING.md rely on are also missing.",
   "change": "Add before `npm run harness`: 'npx playwright install chromium webkit   # once, for the harness'. After `npx tauri build` add:\n'target/debug/job-alert-monitor --dry-run --smoke --smoke-run   # debug smoke probe'\n'cargo test -p jobalert-core --test matching_corpus -- --ignored report --nocapture'\n'cargo test -p jobalert-core --test matching_heldout -- --ignored heldout_report --nocapture'"
  },
  {
   "file": "README.md",
   "where": "line 106, 'Project layout', core/",
   "problem": "The `core/` entry leaves out the profile module (core/src/profile: form, merge with backup, CV prompt, country codes).",
   "change": "'- `core/` - `jobalert-core`: mail scanning, portal fetching, the matching engine, the profile form, storage and export; no UI code, no `unsafe`.'"
  },
  {
   "file": "README.md",
   "where": "'What it does', after line 15 (optional)",
   "problem": "The README never says that inbox jobs move to the archive and the trash empties itself after 30 days. Emptying the trash is a deletion that users should know about.",
   "change": "Add one sentence: 'Jobs live in three places like mail (Jobs, Archiv, Papierkorb); after 30 days jobs that are no favourite move to the archive and the trash empties itself (both switchable in Einstellungen).'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "line 21, Decisions 'Scraping'",
   "problem": "The row still says 'risk badge per switch'. The user removed the risk grades (573fa57). The raised per-portal limits (user, 2026-09-25) are not in any decision.",
   "change": "Replace the cell with: 'everything switchable per portal (Active / Fetch details / Sign in), safe defaults. Amended (user, 2026-09-25): no risk grades, each switch says in one sentence what it does; limits raised moderately to LinkedIn 30 per hour / 80 per day, freelancermap 40 / 120, freelance.de 20 / 60 (`limits()` of each adapter); pace, pauses and the stops on 429/999/403/captcha/login wall unchanged'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "line 28, Decisions 'Layout'",
   "problem": "'(no count: the list's \"Neu n\" says it ...)' can be read as 'no counts anywhere'. The user clarified that the count goes only at Jobs: 'ich meinte jetzt nur bei jobs'. The list's segments keep theirs; the served UI shows 'Neu 6', 'Alle 14', 'Favoriten 1' and no count on nav-jobs.",
   "change": "Replace with '(no count at Jobs: the list's segments Neu n, Alle n and Favoriten n keep their counts; user, 2026-09-25 evening)'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "line 31, Decisions 'UI round 2'",
   "problem": "This row contradicts later decisions and the code. It says 'switches ink when on', but Toggle.svelte is 'coral when on (user decision)' and --toggle-on is p-coral. It says 'primary in deep coral (4.9:1)', but --primary is p-coral 13 73% 63% and the white label is 2.75:1. It says 'sort as icon toggle', but ListHeader uses a MenuButton with the OS menu (Nach Passung, Nach Datum).",
   "change": "Append: ' Superseded since: switches coral when on (user test 2026-09-24); the primary is light coral 13 73% 63% with a white 600 label (2.75:1, the documented contrast exception); the order is a quiet MenuButton that opens the OS menu (Nach Passung, Nach Datum)'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "line 34, Decisions 'UI overhaul after the installed test'",
   "problem": "The row describes day-overview tiles ('tiles Neu and Ohne Details ... a Gemerkt tile'). DayOverview.svelte now has no tiles and no counts: Neu und passend, Offene Punkte, Dateien.",
   "change": "After 'a Gemerkt tile once something is pinned' insert ' (superseded: the day overview has no tiles or counts, see UI \"Jobs\")'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "line 38, Decisions 'cxpertise navy'",
   "problem": "The row says 'the active nav (a sliding white pill with a navy label)', but Warm selection and SideNav use an ink label and a coral icon. It says 'counts (a deep navy pill in the sidebar ...)', but that pill, --count-bg and the Count 'strong' tone were removed in a2b2353. It says 'press ... small scale (0.97 / 0.94 icon / 0.985 tiles)', but only --scale-press 0.98 exists and only Button uses it, which matches Warm selection's 'everything else only darkens'.",
   "change": "Replace 'the active nav (a sliding white pill with a navy label)' with 'the active nav (a sliding white pill; label and icon as in Warm selection)'. Replace 'counts (a deep navy pill in the sidebar, soft pills elsewhere)' with 'counts (soft pills; the sidebar has none)'. Replace 'press 60 ms with a small scale (0.97 / 0.94 icon / 0.985 tiles)' with 'press 60 ms, buttons scale uniformly to 0.98 (`--scale-press`), everything else only darkens'."
  },
  {
   "file": "docs/PLAN.md",
   "where": "line 39, Decisions 'Profile editor'",
   "problem": "Its block list is superseded by line 40, but the row does not say so. It also says 'a German prompt for the user's own Claude', while the prompt now follows the app language and names no product ('für eine KI').",
   "change": "After 'grouped as a consultant thinks (' add 'superseded by \"Profile page, final round\": '. Replace 'a German prompt for the user's own Claude' with 'a prompt in the app's language for any AI chat, no product named'."
  },
  {
   "file": "docs/PLAN.md",
   "where": "line 41, Decisions 'Self-decided'",
   "problem": "'primary button brand-near (coral 56 %, label 600)' does not match --primary (13 73% 63%, the user's light coral).",
   "change": "'primary button light coral 13 73% 63% (user test 2026-09-24), label 600'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "Decisions table, new row after line 40",
   "problem": "The user decisions from the UI logic round appear only as prose in the phase-5 box. The Decisions table has none of them: left button only, a press beside a field ends its focus, one keyboard-only focus ring, the handle grip, no fold, no Jobs count.",
   "change": "Add the row: '| UI logic round (user, 2026-09-25) | only the left mouse button presses anything (right and middle never do; the middle button scrolls, autoscroll on Windows); a press beside a text field ends its focus; one focus ring, only from the keyboard; only the switch itself toggles; no manual sidebar fold (icon rail below 1100 px); no count at Jobs in the sidebar; the only resize handle sits between list and reader (grey 4 x 44 px grip on hover, no line) |'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "lines 59-60, Contracts 'Schema 4'",
   "problem": "The plan says 'Excel gets a \"Status\" column (schema 5: \"Beworben am\" with the date)'. That column was removed in 15fdcee; core/src/export/texts.rs COLUMNS has no status or favourite column.",
   "change": "'Excel shows no mark (since schema 5 the favourite is the only mark and no export column shows it)'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "line 127, IPC 'AppState'",
   "problem": "`risk: low|grey|account` is still sent (view.rs Risk, PortalState.risk) but no screen uses it since the risk grades were dropped. Only gallery.ts and InputBoard.svelte still show a risk badge.",
   "change": "Replace 'risk: low|grey|account,' with 'risk: low|grey|account (unused by the UI since the risk grades were dropped; to remove),'. Or remove the field from view.rs and the gallery (integrator)."
  },
  {
   "file": "docs/PLAN.md",
   "where": "lines 131-137, 'Matching engine'",
   "problem": "The module list includes 'semantic (feature)' and the ladder has 'semantic = 0.5 (feature only)'. No semantic module exists: embeddings were dropped and `assess` ignores the Embedder. The real modules are also missing: atoms, fit, facts, contract, permanent, seniority, focus, roles, wishes, engine, job, ad_facts.",
   "change": "Modules line: 'mod, params, types, lexicon/ (external contract), normalize, profile, job, sections, requirements, atoms, ladder, fit, facts, ad_facts, signals, criteria, contract, permanent, seniority, focus, roles, wishes, relevance, engine, score, explain, legacy, pyre'. In the ladder, replace '· semantic = 0.5 (feature only)' with '· no semantic step (embeddings dropped; the `Embedder` seam stays)'."
  },
  {
   "file": "docs/PLAN.md",
   "where": "lines 153-155, 'Evaluation'",
   "problem": "The plan still says 'K01-K40' with two profiles. The corpus now has K01-K58 with six profiles (fin, it, senior, sap, wish, wishSap; parity 6 x 58 = 348 in matching_corpus.rs), plus seven held-out regression corpora.",
   "change": "'Synthetic corpus in CI (both OS): six profiles (fin, it, senior, sap, wish, wishSap), K01-K58 in TXT contract format, `corpus.json` ...; held-out sets 1-7 as regression corpora with frozen floors (`matching_heldout.rs`).'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "lines 165-166, 'Private gates'",
   "problem": "'Spearman >= 0.55 and > old' contradicts docs/MATCHING.md 'Corpus and gates' and examples/common/metrics.rs. There, the pooled Spearman gate was replaced by relevant-pair Spearman >= 0.50 (MIN_SPEARMAN_RELEVANT) and the concordance gates.",
   "change": "Replace '· Spearman >= 0.55 and > old' with '· Spearman over the relevant pairs >= 0.50 and > old · concordance 0v3 >= 0.95, 0v2 >= 0.85, 1v3 >= 0.80, 2v3 >= 0.70 (replaces the pooled Spearman, see docs/MATCHING.md)'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "line 167, 'Prescore order only if AUC >= 0.7'",
   "problem": "The rule depends on an AUC, but no AUC was ever measured, and the queue already follows the prescore (src-tauri/src/commands/run.rs:163). A scratch test measured it offline at a2b2353: AUC 0.906 over 2,824 held-out pairs (relevant = grade 2-3, not excluded; per set 0.846-0.933).",
   "change": "'Prescore order only if AUC >= 0.7: 0.906 on the held-out labels (2,824 pairs; tuned sets, 2026-09-25), so the queue follows `matching::prescore`.' Commit the measurement as an ignored report test next to heldout_report."
  },
  {
   "file": "docs/PLAN.md",
   "where": "lines 170-172, 'Scraping and sign-in'",
   "problem": "'Risk badges: low (freelancermap) · grey ... · account risk (freelance.de signed in)' is stale (573fa57).",
   "change": "'No risk badges (user, 2026-09-25): each switch says in one sentence what it does. Limits per hour / day: LinkedIn 30 / 80 (pace 4-7 s), freelancermap 40 / 120 (3-5 s), freelance.de 20 / 60 (10-20 s plus 8-20 s dwell in the session window).'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "line 190, UI 'Sidebar'",
   "problem": "'An arrow at the end of the Jobs row, after the count' refers to a count that a2b2353 removed.",
   "change": "'An arrow at the end of the Jobs row (`places-toggle`, ...'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "lines 208-212, UI 'Jobs'",
   "problem": "Several parts are stale. The toolbar is 'Neu n | Alle n · Beste Passung | Neueste'; the code has Neu · Alle · Favoriten and the order menu. Rows are '72 px'; --row-height is 86 (106 with two lines). The reader is a 'card 720 px ... ring 96'; it is now unboxed on the sheet, max 720, with ring 56. The day overview is '3 stat tiles, unread per portal, best 3, pinned'; it now has no tiles or counts.",
   "change": "'- Jobs: list header, row 1 search + Abrufen (the primary; Abbrechen during a run), row 2 Neu n · Alle n · Favoriten n, \"Alle als gelesen markieren\" and the order (a quiet MenuButton with the OS menu: Nach Passung, Nach Datum) · left column run card + list (86 px rows, 106 with a two-line title: ring 40, title with unread dot, meta, reason line, date, status badge only on deviation; excluded grey behind the divider; duplicates as one row) · reader unboxed on the sheet, at most 720 px (ring 56 counting up, band word, n of m must, chips for contract type and hard criteria, reasons met/partial/open/check/violations; hover lights the passage, click scrolls to it) · day overview when nothing is selected (Neu und passend with the comparison prompt, Offene Punkte, Dateien; no counts).'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "lines 217-218, UI 'Einstellungen'",
   "problem": "The plan still lists 'switches with risk badges' and omits Sprache. SettingsView.svelte shows Postfach, Abruf, Portale, Dateien, Sprache, Wartung, confirmed in the served UI.",
   "change": "'- Einstellungen: Postfach · Abruf (auto fetch, archive and trash after 30 days) · Portale (switches with one sentence each, health, quota only >= 80 % or while paused) · Dateien · Sprache · Wartung. First run: full page with three real, self-ticking steps.'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "line 220, UI 'All states per screen'",
   "problem": "'Feedback where the action happened; no toasts.' contradicts the Decisions row 'Toasts' (allowed for short confirmations) and line 243 ('merge into one toast with one undo').",
   "change": "'Feedback where the action happened; toasts only as in Decisions \"Toasts\".'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "lines 232-235, UI components",
   "problem": "The plan says '29 components' and lists SidebarEdge, which is gone. ui/src/components has 35: it adds Chip, ChipInput, MenuButton, SelectionBar, TextArea and JobRow as its own file.",
   "change": "'- 35 components (Badge, BrandMark, Button primary|secondary|ghost|danger|link x sm|md|lg, Card, Chip, ChipInput, Count (soft|plain), Dialog, Disclosure, DragBand (macOS toolbar row), EmptyState, Field, Icon, IconTile, JobRow, ListRow, MenuButton (opens the OS menu), Meter, Notice, ReasonItem, ScoreRing, Segmented, SelectionBar, SettingRow, SideNav, Skeleton, Spinner, Splitter, StatTile, StatusLine, TextArea, TextField, Toast, Toggle, Tooltip). Not: select, checkbox, radio, a page-drawn context menu, window controls.'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "lines 238-239, UI 'Motion'",
   "problem": "'press 60 ms with `--scale-press*`' implies several press scales. Only --scale-press 0.98 exists, used by Button.",
   "change": "'press 60 ms with `--scale-press` (0.98, buttons only; everything else darkens)'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "lines 308-311, Phase 2 box '2B Store, runs, export'",
   "problem": "The box is open, but everything it lists is in the code, including its own exceptions: alsoOn in store/duplicates.rs and view.rs:188; the freelance.de guest teaser in portal/freelance_de.rs, shown as 'Nur Anriss'. The 'template' was dropped by decision ('Vorlage speichern' is gone), and the Excel status column was replaced by the email model.",
   "change": "'- [x] 2B Store, runs, export (schema chain to 5 + WAL; alsoOn; freelance.de guest teaser; LocalMatcher, scoring at JobUpdated + catch-up, Rust-triggered rescore and auto fetch after 6 h; profile summary; Excel grey header and grey excluded rows, no mail address on the Info sheet; HTML overview; TXT byte tests; demo with high/mid/low/excluded. Dropped by later decisions: the template, the Excel status column)'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "lines 315-316 (2D) and 322-324 (Integration)",
   "problem": "The plan still says the prescore hook is open: 2D 'Left for the integrator: AppBackends::prescore -> matching::prescore (neutral until then)' and Integration 'D open (prescore hook not exposed by the fetch queue yet)'. The Integration line also says 'prescore orders the fetch queue', which contradicts itself. The hook is done (commands/run.rs:163, fetch/mod.rs order(), test the_queue_follows_the_prescore_then_recency).",
   "change": "2D: 'Done by the integrator: `AppBackends::prescore` -> `matching::prescore` with the profile.' Integration: replace 'D open (prescore hook not exposed by the fetch queue yet)' with 'D done (the queue follows `matching::prescore`)'."
  },
  {
   "file": "docs/PLAN.md",
   "where": "line 408, UI logic round box",
   "problem": "'harness 819 in both engines': the last green CI (run 36143235064 on f5c185b) reports 820 passed.",
   "change": "'harness 820 in both engines (CI run 36143235064)', or the number of the final CI run."
  },
  {
   "file": "docs/PLAN.md",
   "where": "lines 370, 387, 409, 417, the open phase 5/6 boxes",
   "problem": "The open boxes do not say what is already done or what is left.",
   "change": "Add one sub-line per box with the done / missing items from planBoxes. Example for 409: '      Done: long tasks at 2000 jobs; smoke frame numbers on both OS. Missing: a start-time probe (smoke.rs measures none) and a token-pair contrast test (--text-subtle 4.24:1 on white, 3.91:1 on cream; primary label 2.75:1).'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "lines 444-450, 'Status 2026-09-24 evening'",
   "problem": "The status is stale. The next steps it lists are done (ProfileUnderstanding has packs/years/degrees, the design critique, the icon check), and it predates phases 5-6 progress, engines 4-13, the language switch and the UI logic round.",
   "change": "Replace with '## Status 2026-09-25 evening': 'On main (a2b2353; origin at f5c185b, CI green: rust Windows and macOS, harness 820): phases 0-4 and every box but five, engine 13, German and English, native title bars, the UI logic round. Open: 2B tick (done in code), real runs + private gold set, live canary per portal, start time and contrast, final CI build + first-start guide + hand-over.'"
  },
  {
   "file": "docs/PLAN.md",
   "where": "line 16 vs line 157",
   "problem": "'two realistic invented profiles' (Decisions 'Evaluation data') contradicts 'three realistic composite profiles' (Evaluation) and the four corpus profiles plus the held-out profiles.",
   "change": "Line 16: '... real runs through the app, invented and composite profiles (four in the corpus, up to 15 per held-out set), blind labels by two independent agents + tie-breaker'"
  },
  {
   "file": "docs/MATCHING.md",
   "where": "lines 4-5, intro 'Tests:'",
   "problem": "Only three test files are named. Engine behaviour is also gated in matching_heldout.rs, matching_generalisation.rs, matching_criteria.rs, matching_profiles.rs, matching_wishes.rs, matching_page_facts.rs and countries.rs.",
   "change": "'Tests: `core/tests/matching_legacy.rs`, `matching_corpus.rs`, `matching_heldout.rs`, `matching_generalisation.rs`, `matching_criteria.rs`, `matching_profiles.rs`, `matching_wishes.rs`, `matching_page_facts.rs`, `countries.rs`, `core/tests/common/eval.rs`, `core/examples/common/metrics.rs`.'"
  },
  {
   "file": "docs/MATCHING.md",
   "where": "lines 59-60 and 82, 'Corpus and gates'",
   "problem": "The doc says 'four invented profiles and 52 invented ads K01-K52' and 'parity (4 x 52)'. matching_corpus.rs asserts 348 = '6 profiles x 58 jobs'.",
   "change": "Line 59: 'four invented profiles (plus `wish` and `wishSap`, see \"Version 4 against version 3\") and 58 invented ads K01-K58'. Line 82: '(6 x 58)'."
  },
  {
   "file": "docs/MATCHING.md",
   "where": "lines 133-141, table 'Profile keys'",
   "problem": "The allowed-countries key is not documented. Its reading changed in engine 13 (names become codes).",
   "change": "Add the row: '| `harte_kriterien.laender` | `hard_criteria.countries` | allowed countries: ISO codes or country names (`Deutschland`, `Österreich`, `UK` = `GB`), a list or one text; one unknown entry makes it no rule (`criterionNotUnderstood`), version 13 |'"
  },
  {
   "file": "docs/MATCHING.md",
   "where": "between line 459 (end of 'Version 11') and line 461 ('### Rubric of the Claude check')",
   "problem": "Engine versions 12 and 13, held-out set 7 and the current per-set table are missing. heldout_report's summary_line says it prints 'One row of the per-set table of docs/MATCHING.md', but that table does not exist. The GOLDEN_DIGEST comment also requires a MATCHING.md update with every ENGINE_VERSION.",
   "change": "Insert the four sections from matchingDoc: '### Held-out set 7', '### Version 12: the other listings under an ad', '### Version 13: country names in a profile', '### All held-out sets and the corpus at version 13'."
  },
  {
   "file": "docs/MATCHING.md",
   "where": "line 599, 'Private gold set ... pending (phase 5)'",
   "problem": "The line is still true, but it does not say that the tooling is ready or why the set is pending.",
   "change": "'Private gold set (real ads, blind grades 0-3): pending (phase 5). `export_gold`, `match_eval` and the gates are ready (`core/tests/gold_eval.rs`); no private export exists yet (`core/tests/fixtures/private/` holds only mails/ and pages/).'"
  },
  {
   "file": "docs/MATCHING.md",
   "where": "line 671, 'In the app' row `auswertung/top_matches.json`",
   "problem": "The row still describes schema 1 ('scored jobs of the last mailbox run'). export/top_matches.rs is schema 2: it adds appStatus and firstSeenAt, holds unread or favourite inbox jobs from alert mails of the last 14 days, and no longer depends on the last run.",
   "change": "'| `auswertung/top_matches.json` | for the matching skill (optional stage 2), schema 2: `{schema, generatedAt, rev, jobs[<=10]{key, title, company, location, portal, url, score, band, mustMet, mustTotal, appStatus, firstSeenAt, met, partial, open, checks, txtFile}}`: the best scored inbox jobs that are unread or a favourite (`appStatus` `saved`) from alert mails of the last 14 days, best first; written by every run and 2 s after the last mark |'"
  },
  {
   "file": "CLAUDE.md",
   "where": "'Architecture', the `core/` line",
   "problem": "The module list leaves out `profile/` (form, JSON merge with one backup, CV prompt, country codes), `text/`, `secrets.rs` (keychain), `settings.rs` and the ts-rs generator `view/ts.rs`. All are pub mods in core/src/lib.rs.",
   "change": "'- `core/` (`jobalert-core`, `#![forbid(unsafe_code)]`, no UI prose): `mail/` IMAP scan · `portal/` adapters + registry · `fetch/` queue, HTTP, policy, health · `matching/` pure integer scoring engine · `profile/` profile form, JSON merge with one backup, CV prompt, country codes · `store/` SQLite (schema chain) · `pipeline/` runs (scan → fetch → score → export) · `export/` xlsx, txt, overview html, top_matches.json, AI prompts · `text/` HTML to text, names · `secrets.rs` keychain · `settings.rs` · `view.rs` IPC DTOs (ts-rs, `view/ts.rs`).'"
  },
  {
   "file": "CLAUDE.md",
   "where": "'Commands'",
   "problem": "The matching reports that every engine change needs are not listed. The GOLDEN_DIGEST rule requires updating MATCHING.md with each ENGINE_VERSION, and engines 12 and 13 skipped it.",
   "change": "Add: '- Engine changes: `cargo test -p jobalert-core --test matching_corpus -- --ignored report --nocapture` · `... --test matching_heldout -- --ignored heldout_report --nocapture`; every new `ENGINE_VERSION` gets its section in `docs/MATCHING.md`.'"
  },
  {
   "file": "core/src/matching/mod.rs",
   "where": "lines 49-67, doc comment of ENGINE_VERSION (code comment)",
   "problem": "The version history stops at 11, but the constant is 13.",
   "change": "Append: '12: the other listings a portal shows under an ad (`Ähnliche Projekte`, `Similar jobs`) feed no hard criterion and no seniority (`facts::own_text`). 13: country names in a profile's `laender` become ISO codes; an unknown name is reported, never a code.'"
  },
  {
   "file": "core/tests/matching_corpus.rs",
   "where": "lines 29-33, GOLDEN_DIGEST comment (code comment)",
   "problem": "The comment explains version 11 only, but the digest changed twice after that. The report rows at engines 11, 12 and 13 are identical (checked at 085dbdf, c033c13, a2b2353).",
   "change": "Append: 'Versions 12 (the other listings under an ad) and 13 (country names in a profile) changed only the version line as well.'"
  },
  {
   "file": "core/tests/matching_heldout.rs",
   "where": "lines 207-216, HELDOUT7 floor and its comment (test code)",
   "problem": "The comment says 'the footer ANÜ of another listing excludes K03 for seven profiles, three grade-3 jobs are buried'. Since engine 12 that is no longer true: 5 wrong exclusions and 2 buried. The floors were never raised, although the comment says the engine round would raise them. Engine 13 reaches NDCG@10 0.918, pooled Spearman 0.448, precision 0.989, recall 0.889, 2 buried.",
   "change": "Comment: 'Set 7: first contact at engine 10 (0.900, 12 wrong exclusions, 3 buried); engine 12 reads no hard criterion below the other listings (K03). Left: tooJunior on M04 and J02, the student jobs the labelers exclude by their wage.' Floors: ndcg10 0.91, spearman 0.44, exclusion_precision 0.98, exclusion_recall 0.88, grade3_buried 2."
  },
  {
   "file": "src-tauri/src/platform.rs, ui/src/features/shell/Sidebar.svelte, ui/src/components/Tooltip.svelte, ui/src/lib/actions/tooltip.ts, ui/src/lib/platform.ts, ui/src/components/SettingRow.svelte, core/src/export/top_matches.rs",
   "where": "platform.rs:771; Sidebar.svelte:122; Tooltip.svelte:8; tooltip.ts:7; platform.ts:82; SettingRow.svelte:16; top_matches.rs:3 (code comments)",
   "problem": "Stale comments about removed features. platform.rs:771: 'its View menu holds one item, the sidebar (Cmd+B)', but the menu has no View menu. Sidebar.svelte:122: '// Ctrl+B (Cmd+B on macOS) and the macOS menu fold and unfold the sidebar.' Tooltip.svelte, tooltip.ts and platform.ts use Strg+B and 'Seitenleiste einklappen' as examples. SettingRow.svelte: 'e.g. the risk of a portal switch'. top_matches.rs: 'not sent', a mark that no longer exists.",
   "change": "platform.rs: end the sentence at 'through an Edit menu.'. Sidebar.svelte: delete line 122. Tooltip.svelte, tooltip.ts, platform.ts: use 'Strg+F' / '⌘F' and 'Suchen' as the examples. SettingRow.svelte: 'e.g. Verbunden'. top_matches.rs: 'unread or a favourite, in the inbox, the alert mail at most 14 days old'."
  }
 ],
 "planBoxes": "### \"2B Store, runs, export\" (PLAN line 308): done in code, tick it\n- Done: the schema chain to 5 plus WAL (store/schema.rs SCHEMA_VERSION 5, migrate_2_to_3 to migrate_4_to_5; store/mod.rs:45 `journal_mode=WAL`, `synchronous=NORMAL`).\n- Done: the box's two listed exceptions. alsoOn is in store/duplicates.rs:129 and view.rs:188, shown as \"auch auf ...\" in the UI. The freelance.de guest teaser is in portal/freelance_de.rs:69,121 (TextKind::Teaser, \"Nur Anriss\" in the list).\n- Done: LocalMatcher (pipeline/local.rs), scoring at JobUpdated plus catch-up, the rescore Rust starts itself (pipeline/rescore.rs), auto fetch after 6 h (pipeline::AUTO_FETCH_AFTER, auto_fetch_due, test the_auto_fetch_waits_six_hours).\n- Done: settings (per portal enabled, fetchDetails, loginEnabled; autoFetchOnStart; autoArchiveDays; autoEmptyTrashDays; settings.rs).\n- Done: the Excel file has a grey header (HEADER_GREY), excluded rows in grey, and no mail address on the Info sheet (export/xlsx.rs).\n- Done: HTML overview (export/overview_html.rs); TXT byte tests (header_is_exactly_the_contract, txt_is_blind_to_the_match); demo with high/mid/low/excluded (pipeline/demo.rs:266-282).\n- Dropped by later decisions: the \"template\" (\"Vorlage speichern\" is gone) and the Excel status column (the email model, 15fdcee).\n- Missing: nothing in the code; only the box and its text need updating.\n- Offline: yes, a doc edit only.\n\n### \"Real runs through the app\" (PLAN line 370)\n- Done: run 9 on the test mailbox and its mail fixes (promo mails are not alerts, collection mails, title healing; MAIL_PARSER_VERSION 3; fixtures promo_mails/ and forward_composite.eml).\n- Done: the gold tooling. core/examples/export_gold.rs opens the DB read-only, prints counts only, and with `--blind` writes blind labelling files. core/examples/match_eval.rs runs the gates, with relevant-pair Spearman and concordance since 07777f6. The pure parts are tested in core/tests/gold_eval.rs.\n- Done: seven synthetic held-out sets as regression corpora.\n- Missing: no private gold set yet. core/tests/fixtures/private holds only mails/ and pages/, no gold/.\n- Missing: no blind labels by two agents plus an arbiter on real ads.\n- Missing: no old/new report. MATCHING.md line 599 still says \"pending (phase 5)\".\n- Offline possible: running export_gold against the local app database. It is read-only and needs no live request or credentials; the app DB from the earlier installed runs holds the real texts. Then `--blind`, two blind labelers plus an arbiter, match_eval, and only aggregated numbers into MATCHING.md.\n- Condition: at least 60 labelled jobs, or the result is \"preliminary\". I did not open the database, so the count is unknown.\n- Needs the user: more real ads. That takes a live app run on the user's mailbox; the app reads the app password from the keychain itself.\n\n### \"Live canary per portal (one counted page via `admit`)\" (PLAN line 387)\n- Done: fetch::admit (core/src/fetch/mod.rs:539) counts every request durably before it is sent (test admit_checks_the_pause_first_and_counts_only_real_accesses).\n- Done: pauses, caps and stops on 429/999/403/captcha/wall, with the raised limits (30/80, 40/120, 20/60; c033c13 adjusted the tests).\n- Done: parsers tested against anonymised real-skeleton LinkedIn fixtures (core/tests/fixtures/pages/linkedin_guest_{open,closed,short}.html); freelancermap island and freelance.de on synthetic pages.\n- Missing: no canary exists. `grep -ri canary` finds only PLAN.md.\n- Missing: no recorded live run per portal.\n- Missing: no real-structure freelance.de guest page fixture (PLAN line 386), and the WebKit status path still wants the macos-latest probe.\n- Offline possible: write the canary as an example or debug flag, e.g. `cargo run -p jobalert-core --example canary -- --portal linkedin`. It would take one job link per portal from the local DB or core/tests/fixtures/private/mails, go through `admit` with the real policy.json, fetch once, run the adapter's parser, and print only codes (health, description found, length), never text.\n- Offline possible: test it with wiremock like fetch/tests.rs.\n- Needs the user: the live run itself (one counted page per portal). No credentials are needed for LinkedIn, freelancermap or the freelance.de guest page.\n\n### \"Performance: start time; contrast\" (PLAN line 409)\n- Done: long tasks at 2000 jobs (harness timing.spec.ts, 10/10 in both engines, PLAN line 391).\n- Done: the smoke probe prints frame numbers on both OS. Latest green CI, 36143235064 on f5c185b:\n  - Windows: run 8.3 s then 2.5 s, p95 15.7 ms, 0 long tasks, reader max 31 ms.\n  - macOS 26.6.2: run 8.9 s / 2.5 s, reader p95 35 ms, 0 long tasks.\n  - The earlier run 36136285923 showed one 169 ms long task opening the reader on Windows (the known first text layout).\n- Start time, missing: nothing measures it. smoke.rs records no start timestamp (its INIT/PROBE only check readiness, and the watchdog is 60/130 s). CI redirects the SMOKE lines to a file and prints them after exit, so their log timestamps cannot time the start.\n- Start time, missing: the release probes only check \"up after 12 s\" (Windows) and \"up after 15 s\" (dmg on macOS). main.rs logs only \"start {version} (web view ...)\" and nothing when the window is shown.\n- How to measure: set a `static START: OnceLock<Instant>` first in main().\n- How to measure: log `window shown after {ms} ms` in platform::reveal().\n- How to measure: in smoke.rs print `SMOKE {\"start\":{...}}` once the PROBE reports ready. It would carry the Rust-elapsed ms to reveal and to ready, plus from the page `performance.getEntriesByType('navigation')[0]` (domContentLoadedEventEnd, loadEventEnd), `first-contentful-paint` from a buffered 'paint' observer, and a `performance.mark` when the first app_state answers.\n- In CI: print, don't gate, or gate generously (e.g. ready < 10 s debug).\n- Offline possible: all of the start-time work. Also a local measurement of the release build with `--dry-run` (cold after reboot, warm) on this PC, needing no requests or credentials.\n- Contrast, done: gallery.spec.ts checks body text AA on white, and the consistency audit is ticked with \"documented exception: primary button\" (no ratio written anywhere).\n- Contrast, measured now from the live tokens: --text on white 13.8:1, --text-muted 6.93, --text-label/--link 6.91, --accent-text 5.12, count pill 4.57.\n- Contrast, below AA: --text-subtle 4.24:1 on white and 3.91:1 on cream (dates, hints); the primary label, white on light coral, 2.75:1 (hover 3.42), below 3:1 even for large text. The Windows inactive title is 3.91:1, which is the OS convention.\n- Contrast, missing: a token-pair contrast test (every text token × surface, with thresholds and named exceptions); a darker --p-fg-subtle; the primary-label exception written down with its ratio, or a decision to change it.\n- Offline possible: everything for contrast. The primary colour needs the user's choice, because the user chose light coral.\n\n### \"Final CI builds (artifacts only), first-start guide (SmartScreen, Gatekeeper, keychain), close this plan, hand over\" (PLAN line 417)\n- Done: CI builds on every push to main and publishes nothing (bundles-windows setup.exe; bundles-macos dmg + .app zip; screenshots and logs).\n- Done: latest run 36143235064 (f5c185b) is green. rust windows 11m50s, rust macos 10m36s, harness 820 passed; release probe Windows up after 12 s; dmg install probe on macOS 26.6.2 up after 15 s.\n- Done: README 'Install' briefly covers SmartScreen, Gatekeeper and the keychain prompt after updates.\n- Missing: a2b2353 is not pushed yet (origin/main = f5c185b), so there is no build of the final commit.\n- Missing: no separate first-start guide. It should cover Gmail 2-step verification and creating the 16-letter app password; alerts on the three portals; the macOS keychain prompt (\"Immer erlauben\"); where the work folder and the files are; reset and uninstall. README 'First start' is three lines.\n- Missing: the PLAN status section (lines 444-450) is stale, and the open boxes need their notes.\n- Missing: hand-over notes; the README and PLAN fixes listed in the edits.\n- Offline possible: all of it. Docs, ticking the plan, and the push by the integrator, which triggers the final CI build. No live portal requests and no credentials are needed."
}